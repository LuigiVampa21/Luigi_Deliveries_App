import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fastDeliveries.luigiDeliveries',
  appName: 'Luigi_Deliveries_App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
